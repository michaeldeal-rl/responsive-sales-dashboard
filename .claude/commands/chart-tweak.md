# Tweak chart data or styling

Update the ApexCharts configuration in `js/scripts.js` to match the requested dashboard change.

Rules:
- Keep the chart styling consistent with the existing dashboard theme.
- Maintain realistic labels, series values, and color choices.
- Avoid breaking responsive behavior or chart rendering.
- Check the browser after the change to confirm the chart still loads.
